public enum TokenType {
    ADD, SUB, MUL, POW, // + - * ^
    LBRA, RBRA, // ( )
    VAR, // x
    NUM, // Big Integer
    LSBRA, RSBRA, // [ ]
    EQ, // ==
    EXP, // exp()
    FUNC, // f()
    QUEST, COLON, // ? :
    EOF
}